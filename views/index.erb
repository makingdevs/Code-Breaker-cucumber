<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>CodeBreaker Game</title>
</head>
<body>
  <form action="/" method="POST">
    <label for="guess">Enter your guess:</label><br>
    <input type="text" id="input" name="guess"><br>
    <input type="submit" id="guess" value="Guess">
  </form>
  <% if @results %>
    <p id="output"><%= @guess %></p>
    <p id="message"><%= @win ? 'You win!' : 'Try again!' %>
    <ul id="results">
    <% @results.each do |result|%>
      <li><%= "#{result[:attemp]} - #{result[:guess]}" %></li>
    <% end %>
    </ul>
  <% end %>
</body>
</html>

